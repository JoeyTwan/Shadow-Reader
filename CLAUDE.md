# CLAUDE.md

> 本文件为 Claude Code 参与 Shadow Reader 项目开发提供指导。

---

## 项目背景

Shadow Reader（影子读者）是一个 AI 阅读伙伴项目。

核心理念：不是让 AI 替你读书，而是让 AI 陪你一起思考。

用户上传 PDF 书籍后，AI 深度理解书籍内容，作为思想伙伴与用户进行长期、深度的讨论。AI 会记录用户的观点和思想变化，长期形成用户自己的知识体系。

这是一个个人项目，追求深度而非广度，追求质量而非速度。不急躁，不堆砌功能。

---

## 产品核心理念

在开发任何功能之前，先理解产品的核心理念：

1. **AI 是伙伴，不是工具** — AI 不是"帮我总结"的工具，而是和你一起读完同一本书的朋友
2. **深度优于广度** — 宁可只读透一本书，也不追求同时处理一百本
3. **你的思想是第一位的** — AI 的分析只是起点，真正重要的是用户的观点和思考
4. **长期主义** — 陪伴用户的阅读旅程，记录思想成长

---

## 开发原则

### 1. 优先简单方案，快速验证核心价值

MVP 阶段不做过度设计。能用单文件实现就不用微服务，能用简单查询就不用复杂缓存。

每做一个功能前，先问：这个功能对"AI 思想伙伴"这个核心价值有多少贡献？如果贡献不大，先不做。

### 2. 不过早设计复杂架构

不要为了"未来可能的需求"而设计复杂架构。代码应该服务于当前明确的需求。

当需求真的到来时，再重构——那时候你对问题的理解会比现在深得多。

### 3. 保持代码清晰可维护

代码就是文档。命名清晰、结构简洁、注释写"为什么"而不是"做什么"。

### 4. 所有功能围绕"AI 思想伙伴"展开

这是项目的北极星指标。任何功能的取舍，都用这个标准衡量：

> 这个功能是在强化"AI 作为思想伙伴"的体验，还是把它变成了一个普通的 PDF Chat 工具？

### 5. 避免做成普通 PDF Chat 产品

市面上的 PDF Chat 产品已经很多了。影子读者的不同在于：

- 不是"这本书讲了什么"（总结），而是"你怎么看这本书"（讨论）
- 不是一次性对话，而是长期陪伴
- 不是回答问题，而是启发思考
- 不是替代阅读，而是深化阅读

---

## AI Agent 设计原则

项目中涉及三个核心 Agent 角色。它们的职责边界应该清晰。

### Book Agent（书籍理解 Agent）

**职责**：深度理解一本书的内容。

输入：PDF 书籍文本
输出：结构化的书籍理解模型

应该做到：

- 识别书籍的核心论点、论证结构和关键概念
- 理解章节之间的逻辑关系
- 提取作者的核心观点和论证方式
- 识别书中的重要人物、事件、概念定义
- 建立概念之间的关系图谱

不应做：

- 不应该做简单的关键词提取
- 不应该只生成摘要（那是普通 PDF Chat）
- 不应该替代用户对书的判断

### Conversation Agent（对话 Agent）

**职责**：与用户进行有深度的阅读讨论。

输入：用户的提问或观点 + 书籍理解模型 + 对话历史
输出：有深度的回应

应该做到：

- 根据用户选择的视角切换回应方式：
  - **作者视角**：站在作者立场，解释和捍卫书中的观点
  - **批判视角**：挑战书中的观点，提出不同的看法
  - **思想伙伴视角**：平等讨论，互相启发，共同探索
- 引用书中的具体内容作为讨论基础
- 提出有启发性的反问，而不是直接给出"正确答案"
- 关注用户的思考过程，而不仅仅是用户的提问内容

不应做：

- 不应该只是复述书的内容
- 不应该假装自己是作者（而是"从作者视角分析"）
- 不应该对所有问题都给确定的答案——有时提出问题比给出答案更有价值

### Thinking Model Agent（思想模型 Agent）

**职责**：记录和梳理用户的思想变化。

输入：用户的对话记录、观点、疑问
输出：用户思想模型的结构化表示

应该做到：

- 从对话中识别用户表达的重要观点
- 追踪用户对某个主题的看法是否发生变化
- 识别用户反复出现的关注点和思考模式
- 帮助用户看到自己的思想成长轨迹
- 在适当的时候呈现"你之前是这样想的，现在似乎变了……"

不应做：

- 不应该替用户做判断（"你的想法是对的/错的"）
- 不应该把用户的思想简化成标签
- 不应该在没有上下文时强行总结

---

## 技术决策原则

1. **先跑通核心链路，再优化** — PDF 上传 → AI 分析 → 对话，这条链路先完整跑通
2. **选择成熟的方案** — MVP 阶段不追求技术新颖性，稳定性优先
3. **数据归属用户** — 用户的书籍、对话、思想模型都属于用户，数据可导出
4. **延迟优化** — 性能问题在真正出现时再解决，不要过早优化
5. **文件结构反映功能边界** — 按照"书籍分析 / 对话 / 思想模型"组织代码，而不是按"models / views / controllers"

---

## Git 提交规范

所有 commit message 使用中文。

格式：

```
新增：xxx        — 新增功能或文件
修改：xxx        — 修改现有功能或文件
修复：xxx        — 修复 bug
重构：xxx        — 重构代码，不改变功能
优化：xxx        — 性能优化
文档：xxx        — 文档更新
```

示例：

```
新增：PDF 上传功能
修改：对话 Agent 的视角切换逻辑
修复：对话记录保存失败的问题
重构：提取书籍解析公共模块
```

---

## 当前开发阶段

**MVP 第一阶段 — 基础框架搭建**

当前目标：

1. 搭建 Web 应用基础框架
2. 实现 PDF 上传功能
3. 实现 AI 书籍分析核心链路
4. 实现基础对话功能
5. 保存对话记录

当前不做：

- 用户认证系统（先用简单方案）
- 复杂的权限管理
- 多书籍同时处理
- 高级可视化

---

*让 AI 成为思想伙伴，而不是又一个工具。*
